{
  "name": "password-strength-verification",
  "version": "0.0.2",
  "description": "password strength verification funciton",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel src --out-dir dist"
  },
  "author": "dulianqiang",
  "email": "wildcn@qq.com",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}